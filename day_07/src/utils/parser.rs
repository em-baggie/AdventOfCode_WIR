use crate::src::utils::regex;

pub fn parse_input(file_path: &str) -> Result<Vec<HashMap<i32, Vec<i32>>>, BridgeRepairError> {
    let file = fs::read_to_string(file_path)?;
    for line in file.lines {
        
    }
    Ok(output)
}